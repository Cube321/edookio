<% layout ('layouts/boilerplate.ejs')  %> 
<div class="container">
    <div class="row">
        <div class="col-11 offset-1 mb-3">
            <h5>Registrovaní uživatelé</h5>
        </div>
    </div>
    <% for(let user of users){ %> 
        <div class="row">
            <div class="col-sm-2 offset-1"><%= user.email %> </div>
            <div class="col-sm-2"><%= user.dateOfRegistration %> </div>
            <% if(user.isPremium){ %> 
                <div class="col-sm-1">Premium</div>
            <% } else { %>
                <div class="col-sm-1">-</div>
            <% } %>
            <div class="col-sm-1"><a href="/admin/<%=user._id%>/upgradeToPremium">Upgrade to premium</a></div>
            <hr>
        </div>
    <% } %> 
</div>